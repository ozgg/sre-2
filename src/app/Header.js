import world from '../images/world.svg'

export default function Header() {
    return(
        <header className="app-header">
            <img src={world} alt=""/>
            my travel journal.
        </header>
    )
};
